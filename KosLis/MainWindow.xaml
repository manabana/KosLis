<Window x:Class="KosLis.MainWindow"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="Black"
        Icon="IMGs/KosLisLogo.png"
        FontFamily="{DynamicResource MaterialDesignFont}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KosLis"
        mc:Ignorable="d"
        WindowStyle="None"
        BorderThickness="0"
        BorderBrush="Black"
        ResizeMode="CanResizeWithGrip" AllowsTransparency="True"
        Title="MainWindow" MinHeight="450" MinWidth="800"  Height="480" Width="800" Activated="Window_Activated" Deactivated="Window_Deactivated">
    <Window.Resources>
        <Style x:Key="NavigationBT" TargetType="Button">
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="Width" Value="15"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Background" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="17"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid MouseDown="Grid_MouseDown" Background="#141414">
            <StackPanel FlowDirection="RightToLeft" HorizontalAlignment="Right" VerticalAlignment="Center" Height="15" Orientation="Horizontal" Width="55">
                <Button Click="CloseWindow" VerticalAlignment="Center" Margin="0,0,2,0" Grid.Row="1" Width="14" Height="14" Content="">
                    <Button.Template>
                        <ControlTemplate  TargetType="Button">
                            <Grid>
                                <Ellipse Fill="DarkRed"></Ellipse>
                                <Label Content="{TemplateBinding Content}" HorizontalAlignment="Center"
                               VerticalAlignment="Center"></Label>
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Click="ExpandWindow" VerticalAlignment="Center" Margin="0,0,2,0" Grid.Row="1" Width="14" Height="14" Content="">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <Ellipse Fill="DarkGreen"></Ellipse>
                                <Label Content="{TemplateBinding Content}" HorizontalAlignment="Center"
                               VerticalAlignment="Center"></Label>
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Click="MinimizeWindow" VerticalAlignment="Center" Grid.Row="1" Width="14" Height="14">
                    <Button.Template>
                        <ControlTemplate  TargetType="Button">
                            <Grid>
                                <Ellipse Fill="Orange"></Ellipse>
                                <Label Content="{TemplateBinding Content}" HorizontalAlignment="Center"
                               VerticalAlignment="Center"></Label>
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                </Button>


            </StackPanel>
            <Ellipse x:Name="isActive" HorizontalAlignment="Left" Margin="3.5,0,0,0" Fill="#363636" Height="10" Width="10"/>
            <Grid Width="100">
            </Grid>
        </Grid>
        <Grid Grid.Row="1" x:Name="MainGrid">
            <Frame NavigationUIVisibility="Hidden" x:Name="MainFrame"/>
        </Grid>
    </Grid>
</Window>
